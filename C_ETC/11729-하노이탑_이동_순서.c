/*
#include <stdio.h>

void move(int n, char from, char to) {

    list = {'a', 'b', 'c'};
    char temp = 

    if (n == 1) {
        print("%d %d\n", from, to);
        return;
    }

    else {
        move(n - 1, from, temp);
        move(1, from, to);
        move(n - 1, temp, to);
    }
}

int main() {

    int n;
    scanf("%d", &n);
    move(n, 'a', 'c');

    return 0;
}

*/